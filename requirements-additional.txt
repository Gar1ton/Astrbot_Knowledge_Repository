# 手动可选安装：一次性启用全部扩展功能与开发/测试工具。
# 本文件不会被 AstrBot 插件安装器自动安装。
-r requirements.txt

# Milvus Lite 向量检索
pymilvus[milvus_lite]>=2.5,<3.0

# 本地 Embedding。Linux CPU 部署建议先安装 CPU-only PyTorch，避免下载大型 CUDA 组件。
sentence-transformers>=3,<6

# LightRAG 高精度召回
lightrag-hku>=1.5.0rc1,<2.0.0

# PDF → 干净 Markdown 清洗内核（pinned，不 vendor 源码；pymupdf 为其运行时依赖）。
# 版本号同步备注于 core/managers/markdown_extractor.py::PYMUPDF4LLM_PINNED_VERSION。
pymupdf4llm>=0.0.17,<0.1.0
PyMuPDF>=1.24,<2.0

# Cloudflare R2
boto3

# 开发与测试
numpy
pytest
pytest-asyncio
ruff
mypy
