# tests/mock_data/ — 测试夹具数据

放测试用的**静态夹具**：示例输入、期望输出、样本数据集。

约定：

- 数据与测试代码分离，便于多个测试复用同一份夹具。
- 文件命名表意（`sample_conversation.json`、`expected_summary.md`）。
- 大型/二进制夹具谨慎入库；必要时用生成脚本替代。
- 夹具是**只读输入**，测试不应就地修改它。
